{
  "type": "object",
  "properties": {
    "urls": {
      "type": "array",
      "items": {"type": "string"},
      "description": "URLs to fetch"
    },
    "key_prefix": {
      "type": "string",
      "description": "Prefix for memory keys"
    },
    "wait_seconds": {
      "type": "integer",
      "description": "Wait seconds per page"
    }
  },
  "required": ["urls"]
}
