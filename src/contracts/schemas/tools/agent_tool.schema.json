{
  "type": "object",
  "properties": {
    "task": {
      "type": "string",
      "description": "Task description for the agent"
    },
    "context": {
      "type": "object",
      "description": "REQUIRED data for the agent's input contract. Check the tool description for required fields. For plan_generator_agent, must include: target_url, task_name, collected_information (array of sub-agent outputs).",
      "additionalProperties": true
    },
    "run_identifier": {
      "type": "string",
      "description": "UUID from generate_uuid for validation tracking"
    },
    "expected_outputs": {
      "type": "array",
      "items": {"type": "string"},
      "description": "List of expected output fields from the agent"
    }
  },
  "required": ["task"]
}
