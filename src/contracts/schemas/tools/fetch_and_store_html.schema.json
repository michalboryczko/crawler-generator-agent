{
  "type": "object",
  "properties": {
    "url": {
      "type": "string",
      "description": "URL to fetch"
    },
    "memory_key": {
      "type": "string",
      "description": "Memory key to store the HTML"
    },
    "wait_seconds": {
      "type": "integer",
      "description": "Seconds to wait (default: 3)"
    }
  },
  "required": ["url", "memory_key"]
}
