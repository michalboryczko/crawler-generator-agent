{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "plan_generator_agent_input",
  "title": "PlanGeneratorAgent Input Contract",
  "description": "Input requirements for the Plan Generator Agent - aggregates collected information from sub-agents",
  "type": "object",
  "required": ["target_url", "task_name", "collected_information"],
  "properties": {
    "target_url": {
      "type": "string",
      "format": "uri",
      "description": "Target URL for which the crawl plan is being generated"
    },
    "task_name": {
      "type": "string",
      "description": "Name/identifier for this crawl plan task"
    },
    "collected_information": {
      "type": "array",
      "description": "Aggregated outputs from sub-agents (Discovery, Selector, Accessibility, DataPrep)",
      "items": {
        "$ref": "#/$defs/agentOutput"
      },
      "minItems": 1
    }
  },
  "additionalProperties": false,
  "$defs": {
    "agentOutput": {
      "type": "object",
      "description": "Output collected from a sub-agent",
      "required": ["agent_name", "output", "description"],
      "properties": {
        "agent_name": {
          "type": "string",
          "description": "Name of the source agent (e.g., 'discovery_agent', 'selector_agent')",
          "enum": ["discovery_agent", "selector_agent", "accessibility_agent", "data_prep_agent"]
        },
        "output": {
          "type": "object",
          "description": "Complete output from the agent including all response fields",
          "additionalProperties": true
        },
        "description": {
          "type": "string",
          "description": "Human-readable description of what this agent output contains and how it should be used in plan generation"
        }
      },
      "additionalProperties": false
    }
  }
}
