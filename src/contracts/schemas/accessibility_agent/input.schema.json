{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "accessibility_agent_input",
  "title": "AccessibilityAgent Input Contract",
  "description": "Input requirements for the Accessibility Agent - validates HTTP vs browser requirement",
  "type": "object",
  "required": ["target_url"],
  "properties": {
    "target_url": {
      "type": "string",
      "format": "uri",
      "description": "Main listing page URL to test HTTP accessibility"
    },
    "article_selector": {
      "type": "string",
      "description": "CSS selector for article links (from Selector Agent) - used to verify links exist in HTTP response"
    },
    "extracted_articles": {
      "type": "array",
      "items": {
        "type": "string",
        "format": "uri"
      },
      "description": "Sample article URLs to test HTTP accessibility (from Discovery Agent)"
    },
    "collected_article_urls": {
      "type": "array",
      "items": {
        "type": "string",
        "format": "uri"
      },
      "description": "Alternative field for sample article URLs (from Selector Agent)"
    }
  },
  "additionalProperties": false
}
