{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "main_agent_input",
  "title": "MainAgent Input Contract",
  "description": "Input requirements for the Main Agent - the orchestrator that coordinates all sub-agents",
  "type": "object",
  "required": ["target_url"],
  "properties": {
    "target_url": {
      "type": "string",
      "format": "uri",
      "description": "The website URL to crawl and analyze. This is the starting point provided by the user."
    },
    "output_dir": {
      "type": "string",
      "description": "Optional directory path for output files. Defaults to 'data/' if not specified."
    }
  },
  "additionalProperties": false
}
