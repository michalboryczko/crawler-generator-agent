## JSON EXTRACTION FAILED - RETRY REQUIRED

Your response could not be parsed as valid JSON. Please output a properly formatted JSON response.

**Error:** {{ error_message }}

**Retries remaining:** {{ retries_remaining }}

CRITICAL: Your response MUST be valid JSON. Do not include explanatory text outside the JSON block. Use proper JSON syntax (double quotes for strings, no trailing commas).
