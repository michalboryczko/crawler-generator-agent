## ⚠️ CRITICAL: Output Requirements

### Required JSON Schema
Your final response MUST be valid JSON conforming to this schema:
```json
{{ output_contract_schema | tojson(indent=2) }}
```

### Required Fields
You MUST provide values for: **{{ expected_outputs | join(', ') }}**

Run Identifier: `{{ run_identifier }}`

### Output Rules
1. **Return ONLY valid JSON** - no explanatory text outside the JSON
2. **Include ALL required fields** from the schema above
3. **Validation is automatic** - if your output doesn't match the schema, you'll be asked to fix it and retry (up to 3 times)

### Required: Validate Before Returning
You MUST call `validate_response("{{ run_identifier }}", your_json_response)` before returning your final response.
- `your_json_response` must be the complete JSON object (dict), not a string
- Check for `valid: true` in the result - `success: true` only means the tool ran
- If validation fails, fix the errors and try again

### Example Format
```json
{{ example_json | tojson }}
```
